/* Fakta dan Relasi */
initDistribusi(X,Y).
initDistribusi(2,24).
initDistribusi(3,26).
initDistribusi(4,12).

/* Dynamic predicate */
dynamic(nPemain/1).      
dynamic(NameTurn/2).     

/* Rule */
throw2Dice(X).           
